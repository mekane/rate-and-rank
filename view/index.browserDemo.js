import DataGridView from "./src/DataGridView";
//TODO: in-place action handler that keeps a new DataGrid in memory

export function initGrid(selector) {
    console.log('browser demo grid init');
    console.log(document.querySelector(selector));


}